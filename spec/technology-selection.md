| Core Requirement | Technical Capability Needed | Envoy Proxy | Squid Proxy | HAProxy / NGINX |
|---|---|---|---|---|
| HTTP CONNECT Ingestion | Must accept explicit downstream forward proxy requests. | Fully Supported via upgraded CONNECT connection manager. | Fully Supported (Native forward proxy engine). | Limited (Designed primarily for reverse proxy routing). |
| TLS Interception (MITM) | Must terminate the TLS session midway using a manual wildcard certificate (*.domain.com). | Fully Supported via standard Downstream TLS context. | Supported via SslBump (Heavier overhead, unnecessary for static certs). | Supported but requires complex SNI passthrough looping architectures. |
| Downstream mTLS Auth | Must enforce, parse, and validate client certificates against a trusted CA root. | Fully Supported (require_client_certificate: true). | Supported but complex to configure simultaneously with SslBump. | Fully Supported natively. |
| Identity Handoff | Must extract the client certificate and format it cleanly into upstream HTTP headers. | Industry Leader (Native, standardized X-Forwarded-Client-Cert / XFCC specs). | Manual (Requires custom config lines or external ACL helpers). | Supported via manual variable mapping directives. |
| Zero-Downtime Cert Rotation | Must reload the manual wildcard certificate without dropping active client connections. | Exceptional (Automated natively via background Secret Discovery Service / SDS API). | Poor (Requires full configuration parsing, risk of connection interruption). | Moderate (Requires enterprise editions or manual master-process reloads). |
